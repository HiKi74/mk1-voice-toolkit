# 统一换行，避免 Windows/Linux 协作时整文件 diff
* text=auto eol=lf

# Windows 脚本保持 CRLF，否则部分环境下 .bat 会报错
*.bat text eol=crlf
*.ps1 text eol=crlf

# 二进制文件不做换行转换
*.dll binary
*.exe binary
*.pdb binary
*.wav binary
*.wem binary
*.bnk binary
*.zip binary
